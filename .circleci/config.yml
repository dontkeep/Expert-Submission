version: 2.1

orbs:
  android: circleci/android@0.2.1

executors:
  java17:
    docker:
      - image: 'cimg/openjdk:17.0'

jobs:
  build:
    executor: java17
    steps:
      - checkout
      - android/setup  # Install Android SDK and set ANDROID_HOME
      - run:
          name: Accept Android Licenses
          command: |
            yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses
      - run:
          name: Clone Repository (HTTPS Alternative)
          command: |
            if [ ! -d .git ]; then
              git clone https://github.com/dontkeep/Expert-Submission.git .
            fi
      - run:
          name: Fix Java Certificate Issue
          command: |
            sudo rm -rf /etc/ssl/certs/java/cacerts
            sudo apt-get remove --purge --auto-remove ca-certificates-java -y
            sudo apt-get install --reinstall ca-certificates -y
            sudo update-ca-certificates -f
      - run:
          name: Chmod permissions
          command: sudo chmod +x ./gradlew
      - restore_cache:
          key: android-orb-v1-
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies
      - save_cache:
          key: 'android-orb-v1-{{ epoch }}'
          paths:
            - ~/.android/build-cache
            - ~/.android/cache
      - run:
          name: Run Build
          command: ./gradlew build
      - store_artifacts: 
          path: app/build/reports
          destination: reports
      - run:
          name: Run Tests
          command: ./gradlew lint test
      - store_test_results:
          path: app/build/test-results
      - store_artifacts:  
          path: app/build/outputs/apk/debug/
          destination: artifact-file
