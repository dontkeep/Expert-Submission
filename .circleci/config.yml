version: 2.1

executors:
  android-env:
    docker:
      - image: cimg/android:2023.03
    environment:
      GRADLE_OPTS: "-Dorg.gradle.daemon=false"
      _JAVA_OPTIONS: "-Xmx2048m"
      JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
      ANDROID_HOME: /home/circleci/android-sdk
      ANDROID_SDK_ROOT: /home/circleci/android-sdk
      ANDROID_NDK_HOME: /home/circleci/android-ndk

jobs:
  build:
    executor: android-env
    steps:
      - checkout

      # 🔍 Debug: Check if project files exist
      - run:
          name: Debug File System
          command: ls -la

      # ✅ Ensure ANDROID_HOME exists
      - run:
          name: Ensure SDK Path Exists
          command: mkdir -p $ANDROID_HOME

      # ✅ Accept Licenses (Fix License Agreement Issue)
      - run:
          name: Accept Android Licenses
          command: |
            mkdir -p "$ANDROID_HOME/licenses"
            yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      # ✅ Install Android SDK Components
      - run:
          name: Install Android SDK
          command: |
            $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \
              "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      # ✅ Create local.properties with the correct SDK path
      - run:
          name: Set Android SDK Path
          command: echo "sdk.dir=$ANDROID_HOME" > local.properties

      # ✅ Fix Gradle Wrapper Permissions
      - run:
          name: Fix Gradle Permissions
          command: chmod +x ./gradlew

      - restore_cache:
          keys:
            - gradle-cache-{{ checksum "build.gradle" }}

      # ✅ Run Gradle Build
      - run:
          name: Build Project
          command: ./gradlew assembleDebug --stacktrace

      - save_cache:
          paths:
            - ~/.gradle
            - ~/.android
          key: gradle-cache-{{ checksum "build.gradle" }}

      - store_artifacts:
          path: app/build/outputs/apk/debug

      - store_test_results:
          path: app/build/test-results
